# Архив — возможно удалить (создан 11.05 v1)

> **Создал:** Director, 2026-05-11, по правке Инны в аудите my-architecture.
> **Назначение:** место для документов, которые **возможно** устарели / дублируются / отвергнуты. Финальное решение «удалять / вернуть / оставить в архиве» принимает **только Инна** после ручного разбора каждого файла.

---

## Главное правило

🛑 **НИЧЕГО НЕ УДАЛЯЕМ.** Document сюда переезжает командой `git mv` (с сохранением истории), не `rm`. Из архива в основной репо тоже только `git mv`, не «копировать и удалить».

🛑 **Удаляет только Инна, по факту разбора.**

✅ **Папка не пустеет автоматически.** Если все документы изучены и возвращены / решены — она остаётся пустой, на случай следующих архивных кандидатов.

---

## Как заполнять (когда придёт фаза «двигаем сюда устаревшее»)

Каждый перенос — отдельная строка в таблице ниже + директива в файл `notes.md` (если содержит критичный контекст).

| Дата | Файл | Откуда (полный путь) | Причина | Возможный возврат | Решение Инны |
|---|---|---|---|---|---|
| 11.05 | `inna/architecture.md` v7.0 | `inna/architecture.md` | Версия заменена — поглощён в `inna/architecture-2026-05-11-v1.md` | 7-слойная структура и формулировки слоёв в новом master сохранены 1:1; если найдём пропавший элемент при анализе — вернуть фрагментом | _ожидает разбора_ |
| 11.05 | `inna/architecture-domain.md` | `inna/architecture-domain.md` | Промежуточный синтез после Идеи v2 — поглощён в новом master 11.05 | Доменная карта HUB корня + URL семантические перешли в парный `operational-infrastructure-2026-05-11-v1`; если будет недостача — вернуть | _ожидает разбора_ |
| 11.05 | `inna/operational-architecture-task.md` | `inna/operational-architecture-task.md` | Точка входа в серверную задачу — заменена парным документом `operational-infrastructure-2026-05-11-v1` | Этап-карта 1-8 и список «чего не делаем» перенесены в парный документ 1:1 | _ожидает разбора_ |
| 11.05 | `inna/operational-infrastructure-2026-05-10.md` v1.1 | `inna/operational-infrastructure-2026-05-10.md` | Финальная серверная схема 10.05 — заменена парным документом 11.05 | DNS-карта + способ A/B + слоты Beget + раздел 18 (Cloudflare/deploy-user/nginx-конфиги) перенесены в парный документ 1:1 | _ожидает разбора_ |

**Столбцы:**
- **Дата** — когда перенесено, формат `ДД.ММ`
- **Файл** — имя файла или папки в архиве
- **Откуда** — путь до перемещения, чтобы можно было вернуть на место
- **Причина** — кратко: «устарел», «отвергнут», «дубль», «черновик», «версия заменена»
- **Возможный возврат** — что в этом документе может быть полезным (один-два пункта). Это чтобы Инна при разборе понимала, что не зря лежит
- **Решение Инны** — заполняется когда она просмотрит: «удалить» / «вернуть в `<путь>`» / «оставить в архиве»

---

## Что внутри сейчас

Папка `inna/` содержит **4 файла**, перенесены 11.05 после Документов 1+2+3 сабцикла аудита. Решения Инны ожидаются по итогам всех 6 документов и Фаз 3-7.

## Ещё не перенесено (требует решения Инны при прохождении дальше)

- `inna/architecture-idea-v2.md` — внутри Идея v3, **отвергнута Инной 09.05**. Будем анализировать отдельно — может содержать сильные элементы.
- `inna/architecture-implementation-plan.md` — сам помечен «⚠ УСТАРЕЛО», ссылается на отвергнутую v2. Будем анализировать.
- `ARCHITECTURE-ROADMAP.md` (корень репо) — ссылается на несуществующие документы, говорит про 14 агентов (на самом деле 31). Не трогали до решения Инны.
- (Прочие — определятся в Фазах 3-7 аудита)

Перенос произойдёт **только после согласия Инны** на конкретный список.

---

## Связанные правила в auto-memory

- `feedback_no_deletion_archive_first.md` — основное правило про эту папку
- `feedback_director_decides_plan.md` — Director не принимает решения за Инну на стратегических развилках
- `feedback_document_versioning.md` — версионирование документов: старые версии не удаляются, переезжают в архив только по решению Инны
